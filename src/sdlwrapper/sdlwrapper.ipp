#error Documentation file.

/**
 * @namespace sdl
 * @brief C++ wrappers for the SDL library.
 *
 * This module acts as an interface between the main application and
 * the C-based SDL library. All the functions are enclosed inside the
 * namespace, and a RAII-style classes are provided for all the SDL
 * types used in the project for the memory management everyone expects
 * from a modern C++ application.
 *
 * This module also provides the user with some utility functions
 * related to the SDL library.
 *
 * @note While the namespace used in this module is simply called "sdl",
 *       the module name is "sdlwrapper".
 */
