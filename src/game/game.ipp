#error Documentation file.

/**
 * @namespace game
 * @brief Main game logic.
 *
 * This module contains the game logic, ranging from very basic (as, for example,
 * the main loop) to the very specifics of different game sub-modes (like the
 * dialogue mode).
 */
